const CheckBox = () => {
    return (
        <div>
            <input type="checkbox" id="scales" name="scales" />
            <label htmlFor="scales">I agree</label>
        </div>
    )
}

export default CheckBox
